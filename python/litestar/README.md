# Litestar

Litestar async API.

## Deploy on Pxxl

Select `python/litestar` as the base directory. Pxxl detects **python / litestar** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: python
Port: 8000
```
