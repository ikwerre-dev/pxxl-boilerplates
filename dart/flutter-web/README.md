# Flutter Web

Flutter web static application.

## Deploy on Pxxl

Select `dart/flutter-web` as the base directory. Pxxl detects **dart / flutter** from the committed project files. The static output is written to `build/web`.

```text
Type: static
Runtime: dart
Port: static
```
