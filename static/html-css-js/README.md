# Html Css Js

Zero-build HTML, CSS, and JavaScript starter.

## Deploy on Pxxl

Select `static/html-css-js` as the base directory. Pxxl detects **static / vanilla** from the committed project files.

```text
Type: static
Runtime: static
Port: static
```
