# Html

Zero-build semantic HTML starter.

## Deploy on Pxxl

Select `static/html` as the base directory. Pxxl detects **static / html** from the committed project files.

```text
Type: static
Runtime: static
Port: static
```
