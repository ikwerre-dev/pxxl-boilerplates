# Multipage

Zero-build multi-page website with a custom 404 page.

## Deploy on Pxxl

Select `static/multipage` as the base directory. Pxxl detects **static / html** from the committed project files.

```text
Type: static
Runtime: static
Port: static
```
