# Minimal Api

ASP.NET Core Minimal API.

## Deploy on Pxxl

Select `dotnet/minimal-api` as the base directory. Pxxl detects **dotnet / aspnetcore** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: dotnet
Port: 8080
```
