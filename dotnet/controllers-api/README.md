# Controllers Api

ASP.NET Core controller-based API.

## Deploy on Pxxl

Select `dotnet/controllers-api` as the base directory. Pxxl detects **dotnet / aspnetcore** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: dotnet
Port: 8080
```
