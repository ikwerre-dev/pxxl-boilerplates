# Mvc

ASP.NET Core MVC web application.

## Deploy on Pxxl

Select `dotnet/mvc` as the base directory. Pxxl detects **dotnet / aspnetcore-mvc** from the committed project files. The health check is available at `/health`.

```text
Type: fullstack
Runtime: dotnet
Port: 8080
```
