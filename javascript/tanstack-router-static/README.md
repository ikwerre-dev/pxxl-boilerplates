# Tanstack Router Static

TanStack Router client-side application built with Vite.

## Deploy on Pxxl

Select `javascript/tanstack-router-static` as the base directory. Pxxl detects **javascript / tanstack-router** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
