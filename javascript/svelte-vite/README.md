# Svelte Vite

Svelte SPA built with Vite.

## Deploy on Pxxl

Select `javascript/svelte-vite` as the base directory. Pxxl detects **javascript / svelte** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
