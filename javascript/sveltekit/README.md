# Sveltekit

SvelteKit server application using the Node adapter.

## Deploy on Pxxl

Select `javascript/sveltekit` as the base directory. Pxxl detects **javascript / sveltekit** from the committed project files. The health check is available at `/health`.

```text
Type: fullstack
Runtime: node
Port: 3000
```
