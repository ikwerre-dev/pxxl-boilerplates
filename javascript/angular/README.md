# Angular

Angular standalone application.

## Deploy on Pxxl

Select `javascript/angular` as the base directory. Pxxl detects **javascript / angular** from the committed project files. The static output is written to `dist/browser`.

```text
Type: static
Runtime: node
Port: static
```
