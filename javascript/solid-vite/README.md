# Solid Vite

SolidJS SPA built with Vite.

## Deploy on Pxxl

Select `javascript/solid-vite` as the base directory. Pxxl detects **javascript / solidjs** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
