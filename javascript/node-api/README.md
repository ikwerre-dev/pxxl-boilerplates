# Node Api

Dependency-free Node.js HTTP API.

## Deploy on Pxxl

Select `javascript/node-api` as the base directory. Pxxl detects **javascript / node** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: node
Port: 3000
```
