# Preact Vite

Preact SPA built with Vite.

## Deploy on Pxxl

Select `javascript/preact-vite` as the base directory. Pxxl detects **javascript / preact** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
