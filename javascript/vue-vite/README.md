# Vue Vite

Vue SPA built with Vite.

## Deploy on Pxxl

Select `javascript/vue-vite` as the base directory. Pxxl detects **javascript / vue** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
