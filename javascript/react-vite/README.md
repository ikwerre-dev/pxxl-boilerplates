# React Vite

React SPA built with Vite.

## Deploy on Pxxl

Select `javascript/react-vite` as the base directory. Pxxl detects **javascript / react** from the committed project files. The static output is written to `dist`.

```text
Type: static
Runtime: node
Port: static
```
