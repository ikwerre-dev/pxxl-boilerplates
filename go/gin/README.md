# Gin

Gin Go API starter.

## Deploy on Pxxl

Select `go/gin` as the base directory. Pxxl detects **go / gin** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: go
Port: 8080
```
