# Rails Api

Rails API-only starter.

## Deploy on Pxxl

Select `ruby/rails-api` as the base directory. Pxxl detects **ruby / rails** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: ruby
Port: 3000
```
