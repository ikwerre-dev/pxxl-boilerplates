# Vanilla Api

Dependency-free PHP JSON API.

## Deploy on Pxxl

Select `php/vanilla-api` as the base directory. Pxxl detects **php / php** from the committed project files. The health check is available at `/health`.

```text
Type: api
Runtime: php
Port: 8080
```
